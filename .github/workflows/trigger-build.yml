name: Obsolete Build Trigger

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Informational only
        run: echo "Use Android CI for the Bmax Super Apps build."
